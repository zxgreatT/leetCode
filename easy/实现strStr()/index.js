/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(haystack, needle) {
    var haystackArr = [...haystack]
    var needleArr = [...needle]
    for(var i=0;i<needleArr.length;i++) {
        for(var j=0;j<haystackArr.length;j++) {
            
        }
    }
}